{{/*map[string][]viewModels.OrderGroupByuserItem*/}}
    <div data-page="group-by-item" class="page">
        <div class="page-content">
		{{if not .}}
			<div style="text-align:center;font-size:24px;margin-top:10px;">没有数据!</div>
		{{end}}
		{{range $k,$v:= .}}
			<div class="content-block-title">{{$k}}</div>
            <div class="list-block media-list">
                <ul>
                {{range $v}}
                    <li class="swipeout">
                        <div class="swipeout-content">
                            <a href="/web/detail/{{.id}}" class="item-link item-content">
                                <div class="item-inner">
                                    <div class="item-title-row">
                                        <div class="item-title">{{.name}}</div>
                                        <div class="item-after">待发:<span class="badge">{{.itemCount}}</span></div>
                                    </div>
                                    <div class="item-subtitle">{{.phone}}</div>
                                    <div class="item-text">{{.address}}</div>
                                </div>
                            </a>
                            <div class="swipeout-actions-right">
                                <!-- We add data-confirm and data-confirm-title attributes -->
                                <a href="#" d-id="{{.id}}" class="action1 bg-red" data-confirm="确定删除这条记录?" data-confirm-title="删除确认">删除</a>
                            </div>
                        </div>
                    </li>
                {{end}}
                </ul>
            </div>
		{{end}}
        </div>
    </div>
